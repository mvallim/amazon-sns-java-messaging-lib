name: SonarQube

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
      - release/**
      - develop
    types:
      - opened
      - synchronize
      - reopened

jobs:
  gates:
    uses: ./.github/workflows/ci-gates.yml
    secrets: inherit